#! /usr/bin/bash

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="db-setup"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

BIN_PATH="$(realpath .)/bin"
PROJECT_DIR=$(dirname $BIN_PATH)

echo $PROJECT_DIR

# source "$BIN_PATH/drop"
# source "$BIN_PATH/create"
# source "$BIN_PATH/schema-load"
# source "$BIN_PATH/seed"